import { css } from "styled-components"

export const baseStyle = css`
  :root {
    font-size: 10px;
    line-height: 1.5;
    text-align: start;
  }

  body {
    margin-left: auto;
    margin-right: auto;
    padding: 12px;
    width: 1024px;
  }
`
